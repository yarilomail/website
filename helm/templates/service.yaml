apiVersion: v1
kind: Service
metadata:
  name: {{ include "yarilomail-web.fullname" . }}
  labels:
    {{- include "yarilomail-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "yarilomail-web.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
