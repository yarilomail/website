# image.tag intentionally unset: defaults to Chart.AppVersion, so a
# helm/Chart.yaml version bump is what rolls out a new image.

replicaCount: 1

ingress:
  host: wwwdev.yarilomail.org
  tlsSecretName: wwwdev-yarilomail-org-tls

# Keep staging out of search engines.
noindex: true
